/**
 * Payment feature flags — flip these to true when Paymob is live
 * to re-enable card and Apple Pay options without restoring code.
 */
export const ENABLE_CARD_PAYMENTS = false;
export const ENABLE_APPLE_PAY = false;
