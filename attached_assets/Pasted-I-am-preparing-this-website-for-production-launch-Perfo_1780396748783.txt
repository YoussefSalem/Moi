I am preparing this website for production launch. Perform a complete pre-launch audit of the entire codebase and fix any issues you find.

Objectives:

* Run a full TypeScript type check across the entire project and resolve all errors, warnings, and type mismatches.
* Check all pages, components, layouts, APIs, server actions, database queries, hooks, utilities, and integrations.
* Verify that all imports, exports, dependencies, environment variables, and routes are configured correctly.
* Ensure there are no build errors, runtime errors, console errors, hydration issues, or broken references.
* Check for unused code, dead code, duplicate code, and potential production issues.
* Verify all forms, buttons, links, navigation elements, drawers, modals, popups, dropdowns, filters, search bars, and interactive elements function correctly.
* Test all user flows end-to-end and ensure there are no broken paths.
* Confirm all images, assets, product pages, collections, and dynamic content load properly.
* Validate responsive behavior across mobile, tablet, and desktop viewports.
* Verify all Meta Pixel events are firing correctly and only once where intended, including PageView, ViewContent, AddToCart, InitiateCheckout, Purchase, and any custom events.
* Check analytics, tracking scripts, and third-party integrations for errors or duplicate firing.
* Review SEO implementation, metadata, structured data, canonical tags, sitemap generation, and robots configuration.
* Check performance issues, unnecessary re-renders, large bundles, memory leaks, and inefficient queries.
* Ensure proper error handling, loading states, empty states, and fallback behavior throughout the application.
* Verify production environment compatibility and deployment readiness.

Required Output:

1. Generate a comprehensive audit report.
2. Automatically fix all issues that can be safely fixed.
3. List any remaining issues that require manual review.
4. Re-run type checking, linting, and production build after fixes.
5. Continue iterating until:
    * TypeScript errors = 0
    * Build errors = 0
    * Runtime errors = 0
    * Broken links = 0
    * Console errors = 0
    * Critical warnings = 0

Do not stop at identifying issues. Fix them where possible, validate the fixes, and provide a final production-readiness report with a pass/fail status for every category. The goal is a production-ready website that can be safely published with no known critical issues.