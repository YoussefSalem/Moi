ROMPT (AI AGENT VERSION):

You are working on an existing production web app hosted on Replit.

I want to migrate this project to Shopify Hydrogen (headless storefront) in the safest possible way.

CONTEXT

Current situation:

Project is running in Replit (production is LIVE)
Codebase is NOT currently recognized as a Hydrogen project
I attempted:
npx @shopify/cli hydrogen deploy

and received:

"Looks like you're trying to run a Hydrogen command outside of a Hydrogen project"
Suggests using shopify hydrogen init or --path
CLI warning indicates invalid Hydrogen structure
PRIMARY GOAL

Migrate this project to Shopify Hydrogen WITHOUT affecting the current live production version.

The migration must be:

Safe
Parallel (no downtime)
Reversible until final cutover
Fully tested before switching production
REQUIREMENTS
1. Project Assessment

Determine:

Whether this current Replit project can be converted into Hydrogen
What parts are incompatible (if any)
Whether we should migrate or rebuild Hydrogen from scratch
2. Fix CLI Issue

Fix the error:

"You are running hydrogen command outside of a hydrogen project"

You must:

Identify missing Hydrogen structure
Ensure correct initialization (shopify hydrogen init)
Or explain correct --path usage if applicable
Make shopify hydrogen deploy work correctly
3. Safe Parallel Architecture (CRITICAL)

Design a setup where:

Current Replit app stays 100% unchanged (production locked)
Hydrogen runs in a separate environment (staging/dev)
No traffic is shared between them
No risk of overwriting live system

Preferred approach:

Separate repo OR separate folder structure OR separate deployment target
Clearly isolated environments
4. Migration Plan

Provide step-by-step plan:

Initialize Hydrogen correctly
Connect Shopify store
Move storefront logic into Hydrogen
Rebuild routing/pages if needed
Handle API/data connections
Run local dev preview
Deploy staging Hydrogen
Validate parity with current site
Final cutover strategy (domain switch or proxy switch)
5. Constraints (NON-NEGOTIABLE)
Do NOT modify or break current live Replit deployment
Do NOT assume production can go down
Treat Hydrogen as experimental until final switch
Avoid destructive commands
Prefer duplication over modification
OUTPUT FORMAT

Respond with:

Diagnosis (what is wrong with current setup)
Required fixes for CLI error
Recommended architecture (diagram or structure explanation)
Step-by-step migration plan
Risks / pitfalls to avoid

If anything is unclear about the current setup, ask targeted questions before making changes.